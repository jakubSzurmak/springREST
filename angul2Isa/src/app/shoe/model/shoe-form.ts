/**
 * Represents single edition in form.
 */
export interface ShoeForm {

  /**
   * Shoe's brand's name.
   */
  brandName: string;

  /**
   * Shoe's model.
   */
  model: string;

  /**
   * Shoe's color.
   */
  color: string;

  /**
   * Shoe's size
   */
  size: number;

}
